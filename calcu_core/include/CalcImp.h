#ifndef CALC_IMP_H
#define CALC_IMP_H

#include "Dependence.h"

using std::cout;
using std::string;

class CalcImp
{
    public:
     CalcImp(void);
    ~CalcImp(void);

    int count;
    int total = expression.length();
    string expression;
};

#endif